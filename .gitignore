# Private working files (life plan, finance, issue tracking)
.plans/
.issues/

# Local backups (machine-specific)
backups/

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Scratch artifacts and repros from agent sessions (not shipped)
.logs/

# katgpt-rs is a separate git repository, cloned here for local work
katgpt-rs/
